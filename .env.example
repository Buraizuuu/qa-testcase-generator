# QA Assist does not require any environment variables.
# API keys are configured directly in the app via Settings (⋯ menu → Settings)
# and stored in browser localStorage — never in files.
#
# Supported providers: Ollama (local, free), Google Gemini, OpenRouter, OpenAI, Azure OpenAI
