# Optional: set your Anthropic API key here as an alternative to entering it in the Settings UI.
# The Settings modal (top-right) stores the key in localStorage — no restart needed.
VITE_ANTHROPIC_API_KEY=sk-ant-...
